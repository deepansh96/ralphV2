## Closing: summary draft

[ralph-exchange:{{EXCHANGE_ID}}]

The Frontier is empty. These are the settled decisions (JSON):

{{DECISIONS}}

Draft the decision summary (Markdown), the GitHub issue title, and the issue body (Markdown) for the build-ready issue. State each decision exactly as the Answering Agent decided it.

Reply with Summary JSON only: `{"exchangeId": "{{EXCHANGE_ID}}", "decisionSummary": "...", "issueTitle": "...", "issueBody": "..."}`.
