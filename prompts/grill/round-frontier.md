## Frontier round {{ROUND}}

[ralph-exchange:{{EXCHANGE_ID}}]

Answers from the previous round (JSON):

{{ANSWERS}}

Merge these answers into your design tree, update `CONTEXT.md` and `docs/adr/` inline where a term or ADR-worthy decision is now settled, then send Frontier round {{ROUND}}. Ask only questions whose prerequisites are settled, never re-ask a settled question, and use a `reopens` entry only for a real contradiction. Send an empty `questions` array and an empty `reopens` array when nothing is left to decide.

Reply with Frontier JSON only, with `"exchangeId": "{{EXCHANGE_ID}}"` and `"round": {{ROUND}}`.
